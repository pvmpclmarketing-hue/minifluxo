import React from 'react'; import { AbsoluteFill } from 'remotion'; import { AudioTrack } from './AudioTrack'; import { IntroText } from './IntroText'; import { LyricsOverlay } from './LyricsOverlay'; import { PhotoScene } from './PhotoScene'; import type { VideoTimeline } from './types';
export function MusicVideo({timeline}:{timeline:VideoTimeline}) { return <AbsoluteFill style={{background:'#000'}}>{timeline.photos.map((photo,index)=><PhotoScene photo={photo} key={`${photo.url}-${index}`}/>)}<LyricsOverlay lyrics={timeline.lyrics} animation={timeline.style.captionAnimation}/><IntroText text={timeline.introText}/><AudioTrack src={timeline.audio.url} volume={timeline.audio.volume}/></AbsoluteFill>; }
