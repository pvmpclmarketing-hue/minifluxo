import React from 'react'; import { AbsoluteFill, Sequence } from 'remotion'; import { secondsToFrames } from './timeline'; import { AnimatedCaption } from './AnimatedCaption'; import type { TimedCaption, CaptionAnimation } from './types';
export function LyricsOverlay({lyrics,animation}:{lyrics:TimedCaption[];animation:CaptionAnimation}) { return <AbsoluteFill style={{justifyContent:'center',pointerEvents:'none'}}>{lyrics.map((caption,index)=><Sequence key={`${caption.start}-${index}`} from={secondsToFrames(caption.start)} durationInFrames={secondsToFrames(caption.end-caption.start)}><AbsoluteFill style={{justifyContent:'center',alignItems:'center'}}><AnimatedCaption text={caption.text} animation={animation}/></AbsoluteFill></Sequence>)}</AbsoluteFill>; }
