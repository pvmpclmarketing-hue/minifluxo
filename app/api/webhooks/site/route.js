import { NextResponse } from 'next/server';
import { adminClient } from '../../supabase';
export async function POST(request) { if (process.env.SITE_WEBHOOK_SECRET && request.headers.get('x-site-secret') !== process.env.SITE_WEBHOOK_SECRET) return new NextResponse(null,{status:401}); const body=await request.json(); const phone=String(body.phone||'').replace(/\D/g,''); if(!body.name||!phone||!body.ownerId) return NextResponse.json({error:'name, phone e ownerId são obrigatórios.'},{status:400}); const {data,error}=await adminClient().from('leads').insert({owner_id:body.ownerId,name:body.name,phone,source:'site',music_request:body.musicRequest||null,status:'waiting_pix',provider:body.provider||null}).select().single(); if(error)return NextResponse.json({error:error.message},{status:400}); return NextResponse.json(data,{status:201}); }
