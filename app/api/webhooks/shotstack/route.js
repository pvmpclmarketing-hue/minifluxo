import { NextResponse } from 'next/server';
import { adminClient } from '../../supabase';

export const dynamic='force-dynamic';
const shotstackBase=()=>`https://api.shotstack.io/edit/${process.env.SHOTSTACK_ENVIRONMENT||'stage'}`;

export async function POST(request){try{const {searchParams}=new URL(request.url);const orderId=searchParams.get('order');const token=searchParams.get('token');if(!orderId||!token||token!==process.env.SHOTSTACK_WEBHOOK_SECRET)return NextResponse.json({error:'Não autorizado.'},{status:401});if(!process.env.SHOTSTACK_API_KEY)return NextResponse.json({error:'Integração indisponível.'},{status:503});const payload=await request.json();const renderId=payload?.response?.id||payload?.id;if(!renderId)return NextResponse.json({error:'Notificação sem render id.'},{status:400});const verification=await fetch(`${shotstackBase()}/render/${encodeURIComponent(renderId)}`,{headers:{accept:'application/json','x-api-key':process.env.SHOTSTACK_API_KEY}});const verified=await verification.json();const render=verified?.response;if(!verification.ok||!render?.id)throw new Error('Não foi possível confirmar o render na Shotstack.');const status=String(render.status||'').toLowerCase();const update={updated_at:new Date().toISOString()};if(status==='done')Object.assign(update,{status:'complete',output_url:render.url||null,error:null});else if(status==='failed')Object.assign(update,{status:'failed',error:render.error||render.message||'A Shotstack não conseguiu renderizar este clipe.'});else Object.assign(update,{status:'rendering',error:null});const {error}=await adminClient().from('video_orders').update(update).eq('id',orderId);if(error)throw error;return NextResponse.json({ok:true});}catch(error){return NextResponse.json({error:error.message||'Falha ao processar callback.'},{status:500});}}
